#include <stdio.h>

void Insert(int* A, int ele) {
	int i = ele, temp;
	temp = A[i];
	while (i > 1 && tem)

}

int main(int argc, char const* argv[])
{
	int H[] = { 0,14,15,5,20,30,8,40 };
	int i;
	Insert(H, 10);
	return 0;
}
